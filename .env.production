# Allow your real frontend domain(s)
CORS_ORIGINS=https://app.yourdomain.com,https://www.yourdomain.com

# Cookie/session config
SESSION_COOKIE_NAME=__session
SESSION_COOKIE_DAYS=5
NODE_ENV=production
PORT=8080
# Optionally set cookie domain if using subdomains and want one cookie for both:
# COOKIE_DOMAIN=.yourdomain.com   # (not required; only if you change server code to use it)

# In Cloud Run / Firebase Functions you usually DO NOT need a key file.
# firebase-admin uses the platform's Application Default Credentials.
# Leave GOOGLE_APPLICATION_CREDENTIALS unset in production unless you know you need it.
